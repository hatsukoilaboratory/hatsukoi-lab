# Mini Game Plan

## 両想いとすれ違い（仮）

このゲームは、初恋ラボの9ヒロインを題材にした、順番付き・重複なしのヒット&ブロー風推理ゲームである。公開URLは`/mutual`とし、Reactアプリから独立した単一HTMLとしてCloudflare Pagesで配信する。

| リスク | 対応 | 完了条件 |
|---|---|---|
| CloudflareのSPAフォールバック | `_redirects`に`/mutual`の明示ルールを追加する | 直アクセスとリロードでHTMLが返る |
| スマホの4枠操作 | タップ選択・解除・スロット交換をボタン操作で提供する | 横スクロールなしで一手を作成できる |
| ランダム解答と証言 | 解答から証言の変数を生成する | 3手ごとに有効な手掛かりが増える |
| 将来の差し替え | マスタ・事件・判定セリフをHTML冒頭のデータ領域へ集約する | PNG・台詞・事件をHTML内で置換できる |

## 検証基準

イージーとノーマルの開始、配置・解除・入替、判定履歴、3手ごとの証言、クリア、localStorageの最小手数、X共有URLを確認する。PC幅と390px前後のモバイル幅で画面を撮影する。
