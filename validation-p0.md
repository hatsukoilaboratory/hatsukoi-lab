# P0 検証メモ

## 年次データ駆動化

`/heroines` は `heroineCollections.ts` を参照して、2025年の集合絵＋スポットライトと、2026年のカードを描画する。年次を追加する場合は、年・表示方式・人物・画像・配置・リンクをコレクションデータへ追加するだけでよい。

## 開発用ツール

`/dev/hotspot-editor` で7人分の中心座標ベースの選択領域、移動・右下ハンドルによるサイズ変更、JSON出力を確認した。`/dev/chibi-size-editor` では9人のちびキャラを同時に比較し、スライダーと±操作、設定値の出力を確認した。両ルートは開発環境でのみ表示される。

## データ整合性

`pnpm check:data` と `pnpm check:data:online` はともに成功した。slugの一意性・ヒロインと作品の対応・年次構成・プロフィール必須値・外部URL形式・画像パスと公開ストレージ上の到達性を確認する。
